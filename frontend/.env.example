# Frontend Environment Variables

# In local development, set this to your local backend API URL:
VITE_API_URL=http://localhost:5001/api

# In production (unified Vercel deployment), leave this UNSET.
# The app will automatically fall back to the relative same-origin /api path.
